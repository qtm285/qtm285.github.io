# Everything committed here is uploaded to Pages as-is: the deploy workflow
# publishes `path: .`. Anything that is not meant to be on the web must be
# ignored, not merely left uncommitted.
.venv/
__pycache__/
_site/
